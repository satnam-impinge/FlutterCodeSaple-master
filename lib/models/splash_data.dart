class SplashData {
  final String title;
  final String image;
  SplashData(this.title, this.image,);

  @override
  String toString() {
    return 'splash: {title: ${title},image: ${image}}';
  }
}
